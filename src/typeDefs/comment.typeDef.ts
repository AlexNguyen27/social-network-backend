const typeDef = `
  type Comment {
    id: String
    comment: String
    userId: String
    postId: String
    parentId: String
    createdAt: DateTime
    updatedAt: DateTime
  }

`;

export default typeDef;
